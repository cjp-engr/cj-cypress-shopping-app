describe('Sign Up scenarios', () => {
    beforeEach(() => {

    });

    it('Successfully sign up', () => {

    });

    it('Sign Up failed because the "First Name" is empty', () => {

    });

    it('Sign Up failed because the "Last Name" is empty', () => {

    });

    it('Sign Up failed because the "Email" is empty', () => {

    });

    it('Sign Up failed because the "Password" is empty', () => {

    });

    it('Sign Up failed because the "Password" does not contain "Lower Case",'
        + '"Upper Case", "Digits" and "Special Characters"', () => {

        });

    it('Sign Up failed because the "Confirm Password" is empty', () => {

    });

    it('Sign Up failed because the "Password" and "Confirm Password" do not match', () => {

    });

    it('Sign Up failed because all the fields are empty', () => {

    });
});